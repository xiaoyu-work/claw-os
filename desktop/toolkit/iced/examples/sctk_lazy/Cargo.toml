[package]
name = "sctk_lazy"
version = "0.1.0"
authors = ["Nick Senger <dev@nsenger.com>"]
edition = "2021"
publish = false

[dependencies]
iced = { path = "../..", features = [
    "debug",
    "lazy",
    "wayland",
    "winit",
    "tokio",
    "tiny-skia",
    "advanced",
], default-features = false }
