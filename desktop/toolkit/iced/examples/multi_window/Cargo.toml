[package]
name = "multi_window"
version = "0.1.0"
authors = ["Bingus <shankern@protonmail.com>"]
edition = "2024"
publish = false

[dependencies]
iced = { path = "../..", default-features = false, features = [
    "a11y",
    "tokio",
    "debug",
    "winit",
    "tiny-skia",
] }
