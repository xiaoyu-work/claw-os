# cosmic-store
WIP COSMIC Store

## Claw OS MCP service

The App Host starts `/usr/bin/cosmic-store` with `COS_MCP_SERVER=1`.
`apps/cosmic-store/app.json` is the sole authority for the Store MCP tool
catalog and its capability requirements.
