{
  "id": "cosmic-screenshot",
  "version": "0.1.0",
  "schema_version": 2,
  "name": {
    "en": "Screenshot"
  },
  "summary": {
    "en": "Capture the screen through xdg-desktop-portal and save a PNG."
  },
  "icon": "📸",
  "runtime": "binary",
  "mcp": {
    "entry": "/usr/bin/cosmic-screenshot",
    "transport": "stdio",
    "lifecycle": "lazy",
    "tools": [
      {
        "name": "screenshot.capture",
        "summary": {
          "en": "Capture the screen through the desktop portal and return the saved path, or a cancellation result."
        },
        "args": [
          {
            "name": "interactive",
            "kind": "bool",
            "required": false,
            "default": false,
            "label": {
              "en": "Let the user choose the capture region through the portal UI."
            }
          },
          {
            "name": "modal",
            "kind": "bool",
            "required": false,
            "default": true,
            "label": {
              "en": "Render the portal dialog as modal."
            }
          },
          {
            "name": "save_dir",
            "kind": "path",
            "required": false,
            "default": "~/Pictures",
            "label": {
              "en": "Existing directory for a non-interactive PNG. Defaults to the owner's Pictures directory."
            }
          }
        ],
        "needs": [
          {
            "verb": "fs.write",
            "scope": {
              "kind": "from-arg",
              "arg": "save_dir"
            },
            "when": {
              "kind": "arg-equals",
              "arg": "interactive",
              "value": false
            },
            "why": {
              "en": "Save a non-interactive screenshot in exactly the resolved destination directory."
            }
          }
        ]
      }
    ]
  }
}
