{
  "name": "gateway-discord",
  "version": "0.1.0-scaffold",
  "description": "Discord bot gateway. Exposes the cos agent in a Discord server / DMs; receives messages, forwards to `cos agent ask`, replies with the response.",
  "runtime": "python",
  "commands": {
    "start": "Start the gateway connection (gateway WebSocket)",
    "stop": "Stop a running gateway service",
    "status": "Show running state + recent message stats",
    "send": "Send a message to a channel (positional: channel_id text)"
  },
  "dependencies": {
    "credentials": ["discord_bot_token"],
    "engines": []
  }
}
