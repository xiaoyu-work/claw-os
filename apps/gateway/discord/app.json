{
  "name": "gateway-discord",
  "version": "0.2.0",
  "description": "Discord gateway. `send` posts via the Bot REST API. `start`/`stop` (gateway WebSocket) not yet implemented.",
  "runtime": "python",
  "commands": {
    "start": "Connect to the Discord Gateway WebSocket (NOT IMPLEMENTED)",
    "stop": "Stop a running gateway service (NOT IMPLEMENTED)",
    "status": "Show running state",
    "send": "Send a message to a channel via Bot REST API (positional: channel_id text)"
  },
  "dependencies": {
    "credentials": ["discord_bot_token"],
    "engines": []
  }
}
