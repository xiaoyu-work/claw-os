{
  "name": "gateway-slack",
  "version": "0.1.0-scaffold",
  "description": "Slack bot gateway. Exposes the cos agent as a Slack app; receives mentions / DMs / slash commands, forwards to `cos agent ask`, replies inline.",
  "runtime": "python",
  "commands": {
    "start": "Start the gateway (Socket Mode or Events HTTP)",
    "stop": "Stop a running gateway service",
    "status": "Show running state + recent message stats",
    "send": "Send a message to a channel (positional: channel_id text)"
  },
  "dependencies": {
    "credentials": ["slack_bot_token", "slack_app_token"],
    "engines": []
  }
}
