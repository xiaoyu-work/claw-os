{
  "name": "web",
  "version": "0.2.0",
  "description": "Browser via cos-browser (vendored Obscura) — text/links/JSON output, JS rendering, optional CDP",
  "commands": {
    "read": "Fetch URL, return {url, title, text, links} (or raw HTML / arbitrary --eval result)",
    "scrape": "Fetch many URLs in parallel",
    "screenshot": "Capture a screenshot of a URL (shells out to chromium)",
    "submit": "Submit form data (POST) via urllib"
  },
  "dependencies": {
    "binaries": ["cos-browser"],
    "optional_binaries": ["chromium"]
  }
}
