//! `cos model import <path>` — copy or link a user-provided ONNX/GGUF file
//! into `<models>/<name>/<version>/` and write a manifest.
//!
//! Phase 0.5: stub. Implementation lands with the engines (we need engine
//! metadata to compute the right manifest fields).
