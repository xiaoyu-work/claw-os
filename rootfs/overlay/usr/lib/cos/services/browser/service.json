{
  "name": "browser",
  "description": "cos-browser CDP server (vendored from Obscura). Optional — only needed for external Puppeteer/Playwright clients via ws://localhost:9222. cos app web read works without it.",
  "command": "cos-browser serve --port 9222",
  "workdir": "/var/lib/cos/browser",
  "env": {
    "RUST_LOG": "warn"
  },
  "health": {
    "url": "http://localhost:9222/json/version",
    "interval_secs": 10,
    "timeout_secs": 5,
    "start_grace_secs": 15
  },
  "restart": "on-failure",
  "depends_on": []
}
