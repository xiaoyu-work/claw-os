# Remove live-media and installer-only state from the installed target.
# This runs after GRUB has been configured and before Calamares unmounts.

dontChroot: false
timeout: 30
verbose: true

script:
  - "/usr/lib/cos/init/cleanup-installer-target.sh"

i18n:
  name: "Removing live installer state"
